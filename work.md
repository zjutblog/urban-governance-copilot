# Urban Governance Copilot · 项目复盘学习资料

> 用途：研0 毕业设计 / 就业代表作的前期工作复盘。记录"做了什么、为什么这么做、学到了什么、踩了什么坑"。
> 配套文档：`docs/PLAN.md`（项目规划）、`docs/EVAL.md`（评测报告）、`README.md`（运行说明）。

---

## 0. 一页看懂项目

**一句话**：基于人民网领导留言板 2025 年 33 万条真实留言，构建**多智能体政务治理助手**——群众留言 → 感知分类 → 责任部门归口 → 历史案例检索 → 回复生成 → 质量门控 → 人工复核。

**双线定位**：
- **就业线**：可演示的多智能体系统（工程向）
- **论文线**：NLP 任务（事件分类 / NRE / 归口 / 满意度预测），导师方向偏 NLP

**数据策略（关键务实决策）**：
- 1200 条 LLM 抽取的小样本（12 月 × 100）做**标注实验**——只在这部分花 LLM 的钱
- 33 万条 CSV **现成字段当标签**（回复组织 29.5 万、满意情况 5.9 万）——免费的大规模监督信号

---

## 1. 技术栈与环境

| 组件 | 选型 | 备注 |
|---|---|---|
| LLM 服务 | 云端 DeepSeek API（主）+ 本地 Ollama（兜底/隐私） | 无 GPU |
| 编排 | LangGraph（Plan-and-Execute） | 多智能体 |
| 检索 | BM25（中文 bigram）+ 可选 Chroma 向量 | CPU 友好 |
| 分类 | 本地 BERT / BERT-NER | D:\models\bert-base-chinese-ner |
| 存储 | SQLite（memory.db / trace.db）+ Chroma | 本地 |
| 服务 | FastAPI + uvicorn + 原生 JS 前端（Leaflet 地图） | |
| 环境 | conda env `urban_agent`（Python 3.11.15） | Anaconda 在 D:\anacanda |

**环境脚本**（任何终端免激活，脚本内直接用环境 Python 绝对路径）：
- `.\start.ps1` 启动 Web → http://127.0.0.1:8000
- `.\run.ps1 "留言内容"` 命令行跑一条
- `. .\env.ps1` 手动激活环境

> 踩坑：`.gitigore` 文件名拼错且为空，导致 `.env`（含 API 密钥）和数据会被 commit。已改名 `.gitignore` 并补全规则。

---

## 2. 数据链路（从原始到可用）

### 2.1 原始数据摸底
`D:\人民网留言板2025.csv` 333,470 行，GB18030 编码（Excel 中文导出典型）。字段含：留言类型 / 领域 / 属地 / 留言对象 / 内容 / 回复内容 / **回复组织** / **满意情况** / 三项评分。

摸底脚本：`scripts/inspect_2025.py`、`scripts/stats_2025.py`。

### 2.2 关键发现：`original_id` 就是 CSV 行号
1200 条 LLM 标注 JSON 里的 `original_id` 恰好等于 CSV 的行索引（验证：CSV 行 0 = 留言ID 22253921 = JSON original_id 0）。**因此可以零成本 join 回 CSV**，补全 LLM 没有的字段。

### 2.3 统一数据集
`scripts/build_labeled_1200.py`：1200 JSON + CSV 标签 → `data/labeled_1200.csv`（UTF-8-sig，系统 corpus 直接读）。

| 字段 | join 后覆盖率 |
|---|---|
| 留言类型 / 留言领域 | 100% |
| 回复组织（归口标签） | 90.6% |
| 回复内容 | 95.2% |
| 满意情况（满意度标签） | 19.0%（清 3 条广告水印后） |

**方法论**：监督学习前先审计数据可用性。原始 CSV 自带的列（部门、满意度）就是免费的监督信号，不必用 LLM 标注——这是本项目的成本核心。

---

## 3. 系统架构

多智能体工作流（`app/workflow/graph.py`）：
```
plan → execute → replan（循环）→ quality_gate → human_review
   ├─ 动作白名单：perceive/analyze/retrieve/stats/geo_query/decide
   ├─ harness（预算/超时/安全等级）
   ├─ self-consistency 门控（k=3 采样投票 → pass/retry/escalate）
   └─ trace 全量落库（审计）
```

**可解释性设计**：
- 计划是封闭动作菜单，运行时字段（status/result）LLM 填不了，防污染
- 每步 trace 记录 node/action/latency，可回放
- 决策带 reasoning_chain（可解释推理链）

---

## 4. 核心能力模块（每个做了什么 + 为什么）

### 4.1 感知层 `perception.py`
本地 BERT 事件分类器做粗分类（免大模型 token）。CPU 上快，省成本。

### 4.2 分析层 `analyst.py`（实体理解）
LLM 分析。**重点：结构化实体区分**。
- 字段：`location/time/action/target_entities`
- **few-shot 防倒装句误解**：如"在滨江路**和业主**协商拆迁补偿"——"和业主"的对象是业主不是地点
- 实测：输入"幸福小区**楼下和晚上**有人跳广场舞"，模型正确给出 地点=楼下、时间=晚上、行为=跳广场舞

**方法论**：LLM 实体抽取的偏差往往不是模型傻，是 prompt 没定义清楚"地点/时间/行为/对象"的区别 + 缺 few-shot。给足区分规则和例子，问题即解。

### 4.3 决策层 `decision.py`（防幻觉 v2）
这是本项目的**重中之重**——政务回复的幻觉是红线。
- **证据注入**：强制 responsible_department 从检索案例的真实部门中选
- **强制引用**：related_cases 必须填真实案例 ID，后处理校验剔除伪造引用
- **法规拦截**：policy_references 除非 100% 确定否则留空（编造法规是致命幻觉）
- **回复约束**：只写程序性表述（"已转交相关部门核实处理"），禁止编造"已安排/已处罚"等未发生事实
- **兜底检索**：计划漏掉 retrieve 时自动补一次（`_ensure_docs`）
- **引用溯源校验**：部门/案例 ID 对不上检索结果 → 降 confidence + risk_warning

**最终效果**：3 次采样部门不一致（一致性 0.333 < 阈值）→ 门控自动转人工复核，而非硬给答案。这是政务场景最想要的可信行为。

### 4.4 归口评测（`scripts/eval_department_*.py`）
见第 5 节——本项目最重要的方法论产出。

### 4.5 记忆 `memory.py` / 多轮
- 每轮结束把"诉求→结论"写入 memory（service.py / main.py）
- 前端用 localStorage sessionId，同页面多轮共用一个用户上下文
- **关键**：记忆不只注入 planner，还要注入 analyst 和 decision（否则追问时分析器看不到历史）
- 实测：第二轮追问"还是那个广场舞的事" → analyst 输出"用户**追问**…**此前已反映过**…结合上下文可推断"

### 4.6 地图 / geo（`workflow/nodes.py`）
- `replan_node` 检查：分析出物理地点且计划无 geo_query → **强制插入**（不依赖 LLM 自觉）
- geocode 逐个地点实体尝试 + 省市正则兜底
- 前端失败时提示"未识别到可定位地点"并清空旧标记（不停留在上次地图）

---

## 5. 评测与迭代（数字演进，最有学习价值）

### 5.1 归口评测
| 粒度 / 方案 | Top-1 | Top-3 | 结论 |
|---|---|---|---|
| 机构名（回复组织原值） | 1.7% | 2.8% | 854 类、84.8% 单例 → **小样本下数学上不可学**（理论上限 15.2%） |
| 职能 v1（粗映射） | 24.2% | 60.9% | 关键词覆盖 74.6% |
| 职能 v2（**修映射**） | **37.6%** | **65.0%** | 仅修标签即 +13.4pt |
| BERT 分类器 v1（16类） | acc 25.7% | F1 4.7% | 小类学不动 |
| BERT v2（10类/加权） | acc 28.7% | F1 5.8% | 标签噪声拖累 |
| BERT v3（新映射/加权） | acc 21.0% | F1 21.0% | class_weight 用 acc 换 macroF1 |

### 5.2 三个核心发现（可写进论文/简历）

1. **归口粒度决定成败**
   具体机构名（"XX市XX局"）在 1200 条上 854 类、84.8% 单例——理论上限仅 15.2%。同一职能在不同城市是不同机构（gold=市体育局 vs pred=林口县教育体育局）。
   → **归口必须两级**：城市（可从留言对象/属地免费提取）+ 职能（学习任务）。

2. **标签质量 > 模型质量**
   仅修复职能映射规则（"其他"270→16 条），检索式 baseline 就从 24.2% → 37.6%。此前 BERT 的低分瓶颈是标签噪声，不是模型容量。
   → 监督学习前先审计标签，这是方法论亮点。

3. **小样本下检索式 > BERT 微调**
   1200 条上检索式 37.6% 仍优于 BERT（21-29%）。检索式天然利用"相似留言→相似部门"的历史案例信号。
   → 归口任务以检索式为主干，BERT 留待 33 万全量数据（免费标签）再验证。

### 5.3 职能映射规则 `scripts/dept_rules.py`
- 单一来源，训练 / 评测 / Kaggle notebook 三处共用（改一处即可）
- 23 类函数映射（教育体育 / 住建 / 城管执法 / 生态环境 / 公安司法 / 交通 / 市场监管 / 卫生健康 / 人社 / 民政 / 水务 / 农业农村 / 发改经信 / 应急 / 文旅 / 政务热线 / 能源 / 街道乡镇 / 财政税务 / 通信邮政 / 物业房产 / 党委政府办 / 其他）
- 顺序敏感：职能关键词优先，行政区划兜底（区县/新区/管委会/书记/信访 → 党委政府办）

---

### 5.4 多模块功能与产品化（首页功能大厅，v0.5）

把项目从"单一留言处理"升级为**民情治理平台**，首页 = 功能大厅（卡片墙，点进对应功能）。

**9 个功能模块**（不依赖大模型的可离线跑）：
| 模块 | 说明 | 依赖 |
|---|---|---|
| 智能对话助手 | 留言→分析→归口→回复，SSE 流式思维链 | 大模型 |
| 民情数据看板 | 33 万统计（领域/月份/省份/满意度/高发词） | dashboard.json |
| 区县地图洞察 | 642 区县打点，点击弹常见诉求 | dashboard.json |
| 风险预警 | 26 个涉稳涉急关键词识别 | dashboard.json |
| 相似案例检索 | BM25 搜历史案例 | 检索器 |
| 单条快速分析 | 本地归类/归口/紧急度 | 检索+规则 |
| 批量分析 | CSV 上传→批量归口+满意度→导出 | 检索+规则 |
| 民情月报 | 输入条件→模板生成治理报告 | dashboard.json |
| 决策复盘 | 历史运行回放对比 | trace.db |

**数据底座** `data/dashboard.json`（33 万预处理，零 LLM 成本）：字段/月份/省份/满意度/高发词 + 642 区县聚合 + 300 风险。

**产品化改进**：
- **核心结论前置**：结果顶部先出"诉求摘要/紧急度/预测部门/门控"
- **引用案例可点击**：related_cases → 弹窗看原文（有据可依可视化）
- **思维链浅色**：推理过程（浅灰、标"仅供参考"）与真实结论分区
- **API 首页配置**：`set_llm_config()` 全局覆盖（前端 key 真正生效，之前只影响主链路）
- **对话健壮性**：`make_decision` 解析失败自动重试 → 降级兜底，不再崩

---

## 6. 踩坑记录（对学习者最珍贵的部分）

| 坑 | 症状 | 解法 |
|---|---|---|
| sklearn 分类器崩溃 | `exit -1066598273`（原生崩溃，无 traceback） | 根因：scikit-learn 1.9 estimator 依赖 scipy/OpenMP 二进制与 numpy 2.4.6 不兼容。torch/pandas/bm25 正常，唯独 sklearn fit 崩。**换 LightGBM**（不同实现，稳定） |
| `policy_references=""` 解析崩 | LLM 输出空字符串而非 `[]` | pydantic field_validator 把空串/逗号串/数字列表统一转成字符串列表 |
| `related_cases=[数字]` 解析崩 | LLM 输出整数 ID | `_coerce_list` 元素转 str |
| 多轮记忆"无效" | 追问不连贯 | 记忆只注入 planner，未注入 analyst/decision → 三处都注入 |
| 地图不更新 | 无地点时停留旧地图 | geo 失败时前端提示 + 清空旧标记；replan 强制插 geo_query |
| `.gitigore` 拼错 | 密钥/数据会被 push | 改名补全 |
| PowerShell stderr 误判 | transformers warning 导致 exit 1 | 用 `py -W ignore` 或识别"实际成功" |
| **高德瓦片 404** | 地图空白（`webrd0*.autonavi.com` 不返回瓦片） | 换成 `wprd0*.autonavi.com/appmaptile?lang=zh_cn&size=1&style=7` |
| **视图 display 覆盖 hidden** | 主页下方漏出功能 | CSS `.view[hidden]{display:none!important}`（flex 会覆盖 HTML hidden） |
| **浏览器缓存旧前端** | 改了前端仍显示旧版 | 前端文件引用加 `?v=N` 版本号；Ctrl+Shift+R 硬刷新 |
| **API 402 余额不足** | 对话"运行出错" | DeepSeek 账户余额不足，非代码 bug；充值或换 key |
| **语义向量首次超时** | bge-small-zh 首次构建索引慢 | 语义检索（混合）默认关闭（RETRIEVER_USE_VECTOR=1 才启用），避免拖慢 |

---

## 7. 当前状态 & 下一步

**完成度（60 分目标）**：
- ✅ 数据底座、多智能体骨架、检索、门控、评测、防幻觉 v2、P0 体验、文档
- ✅ **首页功能大厅 + 9 个功能模块**（4 个用大模型 / 5 个本地可离线跑）
- ✅ SSE 流式思维链、库索引可视化、核心结论前置、引用案例可点击
- ✅ 33 万数据底座（dashboard.json）、333 万统计
- ⬜ **DeepSeek 余额充值**（对话功能当前受 API 余额影响）
- ⬜ 小区级地图（高德 POI + NER 抽地点，抽样精做）
- ⬜ 简历 / 论文叙事定稿

**下一步优先级**：
1. **给 DeepSeek 充值**（对话功能恢复的前提）
2. 小区级地图洞察（高德 POI + NER，用 A 方案抽样）
3. 简历叙事 + 数字定稿（把 33 万基线、功能、防幻觉写成简历）

---

## 8. 学习方法论沉淀（写给未来的自己/学弟）

1. **先审数据，再谈模型**：动手前先看标签覆盖率、类别长尾、值域。本项目最大的两个"提升"都来自数据/标签，不是模型。
2. **零成本先出基线**：BM25、join、规则映射都是免费的，先跑出来锚定预期，再决定要不要花 LLM/GPU 的钱。
3. **小样本看能力上限**：1200 条上分类器到头就是 20-30%，别硬调模型——瓶颈在数据量，换 33 万全量才是正道。
4. **防幻觉靠"约束 + 校验"**：单靠 prompt 说"别编造"不可靠；要 prompt 约束 + 后处理校验（部门/案例 ID 对不上就降置信度转人工）。
5. **LLM 输出的字段要容错**：空串、数字、逗号串、缺失……结构化输出的解析必须做防御式容错。
6. **一处规则，多处共用**：dept_rules.py 抽成公共模块，训练/评测/notebook 只改一处——避免三处不一致。
7. **前端改动记得加版本号**：静态文件浏览器会缓存，`?v=N` 强制刷新，否则改了不生效。
8. **"运行出错"先查资费/网络，再查代码**：本次"对话不稳定"其实是 DeepSeek 余额不足（402），不是 bug。遇到报错先看是不是外部依赖（API 余额/网络/缓存）。
